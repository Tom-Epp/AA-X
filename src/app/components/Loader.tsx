const Loader = () => (
  <div
    className="container relative mx-auto flex h-screen min-h-full w-full items-center justify-center"
    data-testid="custom-loader"
  >
    <div className="custom-loader" />
  </div>
);

export default Loader;
